eval_cfg_overwrite:
  datamodule:
    datasets:
      lang_dataset:
        lang_folder: lang_annotations
  model:
    num_sampling_steps: 4

task_embedding_format: clip
log_dir: ./logs/
dataset_path: ./dataset/task_D_D
train_folder: ./checkpoints/flower/${benchmark_name}/.hydra/config.yaml
checkpoint: ./checkpoints/flower/${benchmark_name}/model.ckpt
device: 0
num_videos: 30
debug: False

log_wandb: True
wandb_entity: bennoq

num_sequences: 50
max_steps: 520
n_eval: 20
num_sampling_steps: 4
multistep: 10

benchmark_name: libero_spatial # [LIBERO_SPATIAL, LIBERO_OBJECT, LIBERO_GOAL, LIBERO_90, LIBERO_10]